import MaterialExplorer from '../../components/MaterialExplorer';

const UploadMaterial = ({ sectionId }) => {
  return <MaterialExplorer sectionId={sectionId} canManage={true} />;
};

export default UploadMaterial;
